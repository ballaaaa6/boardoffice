# Historical TV Studio reference evidence — state snapshot, 2026-08-31

This is a historical research snapshot, preserved when the active handoff was shortened after the English/Thai dialogue catalog task. It is not an active handoff, milestone plan, acceptance record for new work, or release manifest. Current status lives only in [HANDOFF.md](<D:/antigravity/board office/HANDOFF.md>) and [ROADMAP.md](<D:/antigravity/board office/ROADMAP.md>).

The notes below retain prior source-analysis findings and links to their local evidence. Their phrases such as “future,” “active,” and “current” describe the point at which those notes were written; consult the root handoff for present implementation status. Generated review images do not substitute for author acceptance.

## Prior source and design observations

- A read-only static review of `00_STARTING_POINT/TV_Studio_Story_v1.2.7_EXTRACTED_ASSETS.zip` was completed on 2026-08-31. The archive remains external research evidence only; it is not a project asset or release input, and no runtime/source/asset behavior changed during the review.
- Confirmed static evidence: `chips.txt` contains 89 object rows with four directional local-tuple groups; 20 rows expose more than one tuple in at least one direction. Examples include two-position Audience Seating, a three-position panel and a six-position board. This supports an orientation-aware object-owned interaction-slot list, but the unnamed tuple fields and exact runtime meaning remain unverified.
- Confirmed static evidence: `body/seb.inf` maps stable numeric action IDs to semantic stand/walk/sit/special-work resources and reuses resources with a transform flag; `s_pc0_u.seb` is a separate two-layer, 12-global-frame work resource and its monitor overlay is another resource. Furniture and desk presentation resources are likewise layered and separate from their image atlases.
- Confirmed static evidence: each recovered map references a base floor image plus three flattened `object_id&variant` grids. This reinforces GDS's existing separation of static world art, placement metadata and navigation; it does not justify replacing the approved Phase 8B world/navigation model.
- Phase 8D locked planning basis: keep actor lifecycle and slot occupancy as synchronized tracks—actor `walking_to_seat/approach/seated_work/exit_seat/walking_from_seat`; slot transition history `free/reserved/occupied/releasing/free`. Reserve atomically before inbound walking, keep inbound/approach reserved, and release before outbound walking. Start with one capacity-one slot per workstation, while defining the contract as a list so future furniture can expose multiple orientation-specific slots.
- Phase 8D locked planning basis: name all GDS fields explicitly (`slot_id`, `transition_gate_uv`, `facing`, `render_owner`, action binding and optional effect channels) instead of importing anonymous tuples. Walking and seated render channels must be mutually exclusive; existing `WorkSeatCore`, chair foreground, VFX and HumanBall composition remain the owners of visual placement.
- GDS already has the semantic character action registry in `CHARACTER/ACTIONS/gds_standard_v1.json`; lifecycle phases should bind to that registry. Optional enter/exit clip hooks may be present but null in the first slice because GDS has no approved sit/stand transition frames.
- A future directional entry/exit lane refinement remains optional and should be added only if GDS crowd/portal visual review demonstrates a real bottleneck. No TV tuple values, timing, scheduler behavior, queue policy, map markers or asset resources should be copied as fact without runtime/code evidence.
- Conversation-specific static evidence: TV Studio separates long modal dialogue (`talk.txt`), short ambient/exclamation text (`text.txt`), notification templates (`evtmsgs.txt`), event dispatch (`events.txt`) and localized text overlays (`language/Television_*.csv`). `events.txt` opcode `2,<index>` resolves the corresponding zero-based `talk.txt` record, while opcode `4,<index>` resolves an `evtmsgs.txt` template. Runtime tags include speaker/partner placeholders, line breaks, colors and portrait references.
- TV Studio's dialogue records are table-driven but not a recovered actor-pair AI: `talk.txt` records use `|` record separators and tab-separated metadata/message pages; `evtmsgs.txt` uses a style/message split; the localization CSV flattens source-table entries into global numeric keys and uses blank-key rows for continuation pages. The unknown numeric metadata and global localization offsets must not be imported as GDS contracts.
- Bubble presentation is also separate from character behavior: `effect_talk.png/.seb` is a small alternating symbolic talk effect, while `fukidashi_base`, `fukibox`, `side_fukidashi` and related resources are composable bubble/UI pieces. `body/seb.inf` exposes stand, walk, sit and work actions but no proven pair-conversation action. Official trailer review confirms ambient actor-anchored bubbles, not the hidden partner-selection, approach, lock or turn state machine.
- Phase 8E design basis: implement an original three-layer system—deterministic conversation coordination, stable-ID dialogue/localization content and independent bubble/effect presentation. The first slice should use two standing actors, atomically lock the pair, route both through existing movement/crowd reservations, face them with existing directional idle actions, recover stamina only on successful completion, then return each actor to its retained WorkSeat assignment. A desk-side variant may later keep the listener seated and use the existing `turn_side_a`/`turn_side_b` work actions, but it is not the baseline acceptance path.
- Author decision on 2026-08-31: use a new GDS one-line dialogue CSV and temporarily reuse TV Studio's `fukidashi_base.png` as six fixed whole-crop presets. Keep `BB1`, `BB2`, `BB3`, `BB4` and `BB6`; explicitly exclude `BB5`. The active project copies the PNG and recovered EN/TH development fonts under `CHARACTER/ASSETS/dialogue/`, with provenance metadata and a mandatory replacement gate before canonical release promotion. `fukibox.png` and `fukibox_1.png` are outside this selected implementation scope.
- Bubble-anchor audit (read-only, 2026-08-31): TV's normal room speech path has no dialogue-data `x,y` position table. `Staff.DrawFukidashi` computes the draw anchor from the live actor render position as `trunc(x_ + ofx_ + cameraOfx), trunc(y_ + ofy_ + cameraOfy) - 20`, then calls `AppData.DrawFukidashiStr`; `Room`/`ObjChip` invokes that path per staff during drawing, so the bubble follows the actor. The enum/ambient path adds only an optional `fukidashi__[3]` vertical offset; no actor bubble-X field was found. `DrawFukidashiStr` is the `fukidashi_base` resource path, while `fukibox` belongs to a separate `DrawFukidashi2` path. No screen-edge clamp/flip rule was recovered in the normal staff path; GDS reuses the `-20` vertical anchor with a visible-face-alpha center-X adjustment and keeps clamp policy as future GDS-owned behavior.
- Author visual cross-check (read-only, 2026-08-31): the supplied TV screenshots agree with the native anchor interpretation—each bubble's tail/visual center tracks the speaking actor's horizontal center, follows the actor while walking, and bobs with the actor's idle render motion. This validates the live-anchor model but does not turn `-20` into a sprite-independent head-top measurement; the actor's own render origin/SEB offsets still determine the exact visible gap.

## Prior bubble and font inspection evidence

- Focused Portal/Crowd/WorkSeat comparison regression after the TV Studio Story review: `39 passed`.
- Phase 8E conversation research audit (read-only): the Game Dev Story and TV Studio Story starting archives were inspected without modifying either archive. TV Studio's `talk.txt`, `events.txt`, `evtmsgs.txt`, `text.txt`, localization CSVs, `effect_talk` resource and scalable bubble resources were cross-checked; Game Dev Story's available package contains visual resources but no equivalent dialogue/runtime tables. No runtime code changed and no new test run was required for this documentation-only audit.
- Focused bubble-slice audit (read-only): `fukibox.seb` defines the expected nine crops from the first `26x26` pixels of image ID 6 using 3-pixel corners/borders and a `20x20` center; `fukitail.seb` defines two tail crops from the `x=26..39, y=0..7` region of the same `fukibox.png`. One PNG therefore contains both the scalable box source and its tail.
- Generated `LOCAL_REVIEW/FUKIBOX_SLICE_REVIEW_20260831/` at the author's request: 9 native nine-slice pieces plus 2 native tail variants, nearest-neighbor 10x inspection previews and `fukibox_slice_manifest.json`. Pixel comparison passed for all 11 native crops; the source hash remains unchanged. This is review-only output and no runtime/source asset was changed.
- Generated `LOCAL_REVIEW/FUKIBOX_HELLO_WORLD_REVIEW_20260831/` at the author's request: one clean and one safe-area-guided 4x nearest-neighbor contact sheet with 18 cases covering 1–5 horizontal center repeats, 1–3 vertical repeats, font sizes 9/16, wrapping, text anchors and tail A/B placement. The exact embedded `M+ 1p medium` font was used; the manifest reports all text cases inside the authored safe rectangle, zero opaque magenta pixels, and an unchanged source hash. This remains review-only; no runtime or source asset changed.
- Bubble sizing follow-up (read-only): the recovered TV text/metadata files expose the `fukibox`/`fukitail` slice records but no dedicated max-width, font-size, line-height or word-wrap rule. Therefore nine-slice proves the box can be composed beyond its source `26x26` sample, while text measurement, wrapping, minimum/maximum dimensions and overflow policy must be authored by the GDS renderer.
- Font/layout follow-up (read-only): the APK hash matches the TV archive README. The recovered Unity font objects identify `default-en-switch` as `M+ 1p medium` (serialized size `16`, line spacing `15.625`) and `default-th` as `Noto Sans Thai` (serialized size `16`, line spacing `21.953125`); both use embedded dynamic TTF data. The IL2CPP metadata/native checks expose runtime `TextLayout` measurement and blocks (`WidthMax`, width/height, line spacing, `StringWidth`, `_makeTextBlocks`) plus `Balloon` controls for text, font size, width max, scale, SEB, padding, tail, shadow, clipping and repeatable/template drawing. Generic `Balloon.Init` defaults its runtime font-size field to `9`, but that is not proof that every dialogue path uses size 9. The direct `DrawTalkBox` path uses `RESCOM_FUKIBOX=8`, 3-pixel corners/borders and 20-pixel repeated edge/center samples. Exact caller max-width, padding and wrap formula remain unresolved; no runtime/source changes or tests were required.
- Presentation-path distinction (read-only): the original has multiple bubble routes—generic `Balloon` uses `IApplication.Seb.Balloon=3`/`kairolib/balloon`, `DrawFukidashiWindow` uses `RESCOM_FUKIDASHI_BASE=6`, and `DrawTalkBox` uses `RESCOM_FUKIBOX=8`. GDS currently uses the author-selected TV `fukidashi_base.png` whole-crop route as a provenance-tagged development skin; this must not be described as proof that every original TV speech display used that one image.
- `fukidashi_base` corrected evidence: the `192x40` PNG contains six already-complete bubble drawings (four above, two below), which can be cropped and drawn whole at fixed native sizes without cap/body assembly. Tight PNG-derived sizes are `71x20`, `57x20`, `41x20`, `23x18`, `41x18` and `49x20`; the fifth drawing has no tail in the original. These six bounds come from visible image/alpha boundaries, not a recovered six-frame metadata table.
- Its companion `com/fukidashi_base.seb` separately defines a four-part adjustable-width recipe, all from the first drawing and image ID `24`: left cap `4x18` at `(0,0)`, right cap `5x18` at `(66,0)`, body `30x18` at `(4,0)`, tail `3x5` at `(34,15)`. Native `DrawFukidashiWindow` (resource ID `6`, RVA `0x7F5958`) draws the two caps, repeats the body with a partial final crop, then optionally draws the tail. This does not mean the PNG contains only four usable pieces. The guide explains SEB composition generally; scanning 81 standard `com` SEBs found only these four image-24 references, with `smoke.seb` unsupported by that parser. No six-preset selection rule or claim that every original runtime path uses the six whole drawings is established.
- Generated `LOCAL_REVIEW/FUKIDASHI_BASE_WHOLE_BUBBLES_20260831/`: six separate native PNGs, x6 nearest-neighbor previews, a numbered contact sheet, source-coordinate/provenance manifest and ZIP. Fresh checks passed for all six exact RGBA crops, coverage of every nontransparent source pixel exactly once, full-atlas RGBA reconstruction and unchanged source SHA-256. The earlier `LOCAL_REVIEW/FUKIDASHI_BASE_AUDIT_20260831/` remains a four-part SEB-component study, not a whole-PNG inventory or a verified original-layout renderer. The active runtime now consumes an exact copy at `CHARACTER/ASSETS/dialogue/fukidashi_base.png`; its project-owned registry excludes BB5 and records the GDS selection/anchor policy.
- Generated `LOCAL_REVIEW/BUBBLE_ANCHOR_PROBE_20260831/`: review-only `fukidashi_base` whole-crop fit sheet plus clean/diagnostic GIFs for `RND_F_004`. The probe covers 26 standard action requests / 51 frame occurrences, raw `M30–M33`, movement tracking and idle bob (95 logical frames). The test text measures 62 px, selects BB1 at `71x20`, and passes exact tail-anchor, idle-bob, movement-delta and opaque-magenta checks; clean GIF encoding is 94 frames from 95 logical frames because one identical pair is duration-coalesced, while diagnostic GIF encoding is all 95.
- Generated `LOCAL_REVIEW/FUKIDASHI_BASE_A_TEST_20260831/` as a review-only capacity probe: all six whole crops receive the same 13-character `aaaaaaaaaaaaa`, centered with recovered blue text color and embedded `M+ 1p medium` at render size 9. The sheet also tests counts `1/5/9/13/17/21`; under the explicitly authored four-pixel horizontal and three-pixel vertical safe inset, the rough repeated-`a` fit counts are `12, 9, 6, 3, 6, 8` for bubbles 1–6. The 13-character sample is `65px` wide and is marked overflow against every tested safe rectangle. These are calibration results only: real words must be selected by measured rendered pixel width, and the probe does not change runtime/source assets.
- Generated `LOCAL_REVIEW/FUKIDASHI_BASE_TEXT_MEASURE_20260831/` as a follow-up pixel-width probe for `Hello!!`: with the same `M+ 1p medium` render-size-9 baseline, the measured advance width is `28px`. It fits bubbles 1, 2, 3, 5 and 6 against safe widths `63/49/33/33/41px`, overflows bubble 4's `15px` safe width, and selects bubble 3 as the smallest allowed option after the current bubble-5 exclusion. The contact sheet, six native renders, recommended bubble-3 render, manifest and ZIP are review-only; production selection must measure each locale string with its actual font/size.
- Generated `LOCAL_REVIEW/FUKIDASHI_BASE_THAI_TEXT_MEASURE_20260831/` as an initial review-only Thai measurement probe using the recovered `Noto Sans Thai` at render size 9 and the same safe inset. `วันนี้อากาศดี` measures `47px` and selects BB2; `สวัสดี` measures `22px` and selects BB3. The `หิวข้าวจัง..!`/`51px`/BB1 result from this folder is superseded because the Thai-only path rendered ASCII punctuation as missing-glyph boxes.
- Thai probe diagnosis: the embedded `default-th`/`Noto Sans Thai` TTF has no usable ASCII glyphs for `.`, `!`, `?`, `,`, Latin letters or similar symbols in the Thai-only probe; Pillow therefore renders its missing-glyph box when the whole string is sent through that font without fallback. The previous `51px` measurement for `หิวข้าวจัง..!` includes three 6px missing-glyph boxes. Pillow is also running without RAQM/HarfBuzz (`features.check('raqm') == false`), so Thai shaping/fallback must be handled by the final renderer before probe values become production measurements.
- Generated `LOCAL_REVIEW/FUKIDASHI_BASE_THAI_TEXT_FALLBACK_20260831/` as the corrected review-only probe: Thai runs use `Noto Sans Thai`, ASCII runs use `M+ 1p medium`, and the complete line is measured/centered after composing both runs. A visual follow-up found that top-aligning the two font runs made ASCII punctuation float; the compositor now uses one shared left-baseline anchor (`ls`) for measurement and drawing. The corrected results remain `วันนี้อากาศดี` `47px` → BB2, `สวัสดี` `22px` → BB3 and `หิวข้าวจัง..!` `42px` → BB2 (BB5 excluded). The corrected contact sheet, 18 native renders, 18 x6 previews, recommended renders, manifest and ZIP contain no missing-glyph boxes for the tested ASCII punctuation; source hash and runtime remain unchanged. This remains a review probe until the final renderer supplies Unity-equivalent Thai shaping and fallback.
- Added a table-fit follow-up in the same review folder: Bubble 1–6 now render `12/9/6/3/6/8` lowercase `a` characters respectively, centered with the same recovered `M+ 1p medium` size-9 / blue-text baseline. All six cases fit their authored safe rectangles (`60/45/30/15/30/40px` text widths versus `63/49/33/15/33/41px` safe widths). `fit_counts_all_six_contact_sheet.png`, six separate PNGs, `fit_counts_manifest.json` and `fukidashi_base_fit_counts_probe.zip` are review-only outputs; the source hash and runtime/source assets remain unchanged.
