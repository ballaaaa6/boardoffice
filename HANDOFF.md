# GDS Central Game Core — Handoff

**Updated:** 2026-09-01 (Asia/Bangkok)
**Project root:** D:\antigravity\board office
**Status:** PHASE8C_CLOSED__CEO_DEPTH_CLOSED__PHASE8D_CLOSED__PHASE8E_DIALOGUE_CATALOG_AND_MOVEMENT_APPROVED__ONE_LOOP_BUBBLE_TIMING_IMPLEMENTED__DIRECT_HEAD_ANCHOR_BUBBLE_VISUAL_APPROVED__PC_CELL_ANIMATION_INTEGRATED__TIMING_POLICY_360_240_240_APPROVED__ACTOR_SNAPSHOT_SPEECH_HOME_RETURN_AND_RUNTIME_PRESENTATION_LOOP_IMPLEMENTED__HOST_BOUNDARY_ADAPTER_IMPLEMENTED__EXTERNAL_APP_VISUAL_QA_NUMERIC_TUNING_AND_AUTHOR_ACCEPTANCE_PENDING
**Next engineering task:** PHASE8E_EXTERNAL_APP_HOST_EMBEDDING_AND_AUTHOR_ACCEPTANCE
**Latest author-directed task:** The author approved the conversation movement principle, the initial presentation loop, the refreshed direct-anchor bubble GIFs and the canonical `60/360/240/240ms` WorkSeat timing policy. Central now owns the read-only UV talk-spot resolver, JSON-safe conversation snapshot/reducer, atomic participant/talk-slot locks, deterministic employee `standing_pair`, employee-to-CEO `ceo_front`, explicit `self_talk`, derived outward-only `seated_host`, pair/self-talk dialogue selection and an assignment-safe timing seam. The active exchange is one loop: first bubble at the shared arrival boundary, partner at +500ms, both bubbles remain through 4,000ms from the first bubble, fade together for 300ms, then visitors return. A seated host holds its direction-named `turn_side_<direction>` Work pose; CEO outbound remains refused. Dialogue bubbles now use the exact head-derived position with no displacement, collision search or connector; the later speaker is painted last and may overwrite an earlier bubble. Seated anchors use the composed WorkSeat human offset. Explicit legacy `talk_frames` callers retain the old compact review path. Standing endpoints prefer V (`u` equal, `v` gap 4), require a two-cell open ring and inverse idle facings; CEO endpoints come from the existing red/front depth envelope; assignments remain immutable and no geometry changed. The separately approved PC source-sheet recovery is now integrated into WorkSeat as an independent presentation channel. The renderer-agnostic actor reducer now owns deterministic 60ms route samples for wander and home/return, canonical portal fade, `home_recovery` stamina restoration, explicit ready-gated `request_return`, same-gate/workseat re-entry and assignment retention. Weighted `talk`, VFX, HumanBall and movement events emit renderer bindings; Central forwards talk, reception-depth and workseat-reentry boundaries to the independent speech lane with their effective timestamps. Speech still owns the five solo pools, `conversation_open` → `conversation_reply`, greeting/work-start/fatigue/leaving one-shots, deterministic 30–60s/45–75s intervals, 2–3s spawn greeting, one floor lane through the 4.3s bubble/fade window, valid three-mode selection, arrival-boundary bubbles and standing-pair shared d6 (`even=happy`, `odd=sad`) followed by the 1.2s emotion hold and return hook. Speech timing and pose/action state remain separate; no stamina or workstation mutation comes from presentation. Central exposes the read-only `gds.runtime_presentation_snapshot.v1`; `RuntimePresentationRenderer` consumes it with per-assignment character/VFX/HumanBall/PC frame channels, walking-depth masks, direct head-anchor bubble paint and lifecycle-route preservation, and `RuntimePresentationLoop` now provides the stateful host adapter that advances, renders and returns events atomically per tick. The QA harness drives that loop across all three pair modes plus home/return and writes review artifacts under `LOCAL_REVIEW/PHASE8E_RUNTIME_PRESENTATION_QA_20260901/`. Live embedding in the app host, visual/behavior author acceptance and numeric sad/happy stamina tuning remain open. No release was promoted.
**Active handoff:** this file only. ROADMAP.md is the single active milestone plan.

## Accepted foundation and current runtime

- The unpacked root is authoritative. 00_STARTING_POINT/ is immutable. Its TV Studio Story files remain source evidence; runtime never reads them.
- Phase 8B world/navigation is approved and frozen. Phase 8C portal lifecycle, the narrow CEO-desk render-depth correction and Phase 8D single-actor WorkSeat lifecycle are implemented, author-approved and closed.
- The accepted release remains releases/GDS_CENTRAL_GAME_CORE_v1.8.5.zip (738 entries; fresh-extract 179 passed; required audits passed; release_clean=true). No new release or phase closeout occurred in this task.
- Employee metadata contains 604 instances (302 Wave 1, 302 Wave 2), 219 initial workstation owners, 83 unassigned Wave 1 and 302 unassigned Wave 2 employees. Stable movement/stamina profiles and employee bridges exist; temporary absence retains ownership and never auto-fills vacancies.
- Dialogue presentation uses fixed complete fukidashi_base crops: BB1/BB2/BB3/BB4/BB6, excluding BB5. It measures locale text at 9 px, rejects overflow, and follows visible face-center X, actor movement and frame bob with the frame-top-minus-20 vertical policy. Thai/ASCII runs share a baseline. The caller supplies the character/employee ID, frame, current actor position and dialogue ID/locale; Central returns the bubble image and placement. Presentation does not choose when or what an actor says.
- The first conversation movement/presentation slice is implemented in `RUNTIME/conversation_spot_core.py` and `RUNTIME/conversation_behavior_core.py`. It keeps assignment ownership immutable, reserves participants/spot before leave-seat, emits `walking_depth` tracks, snaps arrivals to inverse idle facings, selects `conversation_open` → `conversation_reply` pair lines (or a general self-talk line), holds one loop of bubbles for 4,000ms from the first bubble, fades 300ms, then returns to the owned workstation. The actor reducer now adds persistent JSON-safe stamina drain/recovery, weighted event starts, deterministic wander/home/return routes and presentation-channel events. `CentralGameCore.resolve_runtime_presentation` plus `RuntimePresentationRenderer` now provide the read-only cross-channel render snapshot and a concrete consumer: tracks/bubbles are overlaid without mutating actor clocks or assignments, walking actors receive depth masking, and WorkSeat assignments honor independent character/effect/HumanBall/PC frame indices. Lifecycle bubbles preserve the actor's live route pose. Directional turn semantics are named directly: `SE` and `NW` use `turn_side_sw=V+→SW` / `turn_side_ne=V-→NE`; `SW` uses `turn_side_se=U+→SE` / `turn_side_nw=U-→NW`. Dashboard UI/persistence, queues, auto-staffing and additional needs systems remain outside scope.
- Existing pre-task working-tree changes were preserved. No floor placement or navigation geometry was edited; the PC source-sheet cells were intentionally recovered into the canonical world registry while the current authored slot data remains three-way and static floor hashes stay protected.

## Completed editable catalog integration

- The author explicitly requested importing the prepared reference phrases and being able to edit/add them later. This authorizes development content integration; it is not final visual/behavior acceptance or release promotion.
- CHARACTER/DIALOGUE/dialogue.csv is the active editable content source: the original 204 imported phrase IDs plus 800 author-approved expansion IDs, each in EN/TH, plus the preserved hello_world_test/en row = 2,009 rows / 1,005 total IDs.
- Keep each dialogue_id stable while editing text; locale and line_index distinguish localized turns. New project-authored IDs/categories can be added without changing code.
- Optional columns category, usage_scope, enabled, full_text, source_id and source_text extend the original five-column CSV format compatibly. text is the actual display draft; full_text preserves the full locale text and source_text preserves exact original English.
- Before the expansion, imported office rows enabled initially were TH 136 and EN 102; including the legacy test there were 239 enabled rows. The approved expansion added 1,600 office rows: 1,108 fit the current locale renderer and are enabled, while 492 overflow rows remain stored with `enabled=false`. The current catalog therefore has 1,347 enabled rows; these are measured content counts, not hard-coded limits.
- CHARACTER/DIALOGUE/reference_import.json records source files/hashes, the original reference catalog hash, initial categories and import policy. It does not regenerate or overwrite later CSV edits, and is not a live count manifest.
- Central and CharacterSystem expose list_dialogue_lines(locale, category, usage_scope, enabled_only) with JSON-safe metadata. Listing without filters includes disabled rows; callers choosing office content must explicitly filter scope/category/enabled status.
- reload_dialogue_content() parses and checks a candidate catalog, validates all enabled text against the current renderer, then replaces the in-memory catalog. Duplicate IDs, ambiguous CSV columns, malformed enabled flags, enabled markup/placeholders and overflow reject the reload while preserving the old in-memory data. The disk file is not rolled back.
- All dialogue-ID render paths (CharacterSystem frame, Central character and Central employee) reject disabled rows. `resolve_dialogue_line()` can still inspect them, or enforce `require_enabled=True`; conversation selection now uses only enabled office rows and remains deterministic. Dialogue rendering remains side-effect free; stamina mutation and weighted event scheduling live in the separate actor reducer and are not triggered by render calls.
- CHARACTER/DIALOGUE/README.md explains edits, additions, UTF-8/CSV quoting, defaults, API usage and limitations. CONTRACTS/central_contract.json and ROADMAP.md describe this authorized extension.
- The earlier research catalog stays at LOCAL_REVIEW/TV_STUDIO_DIALOGUE_REFERENCE_20260831/. Its README now points to the active catalog. Long talk/notification/UI tables were not bulk-imported as ambient speech. Earlier detailed source/font/anchor research remains in docs/history/TV_STUDIO_REFERENCE_STATE_SNAPSHOT_20260831.md, not another handoff.

## Review-only dialogue expansion draft

- LOCAL_REVIEW/DIALOGUE_CATALOG_DRAFT_20260831/dialogue_draft.csv was the review source for 16 office categories × 50 new EN/TH pairs = 800 pairs / 1,600 rows. Its IDs were appended to the active catalog after author approval; the review folder remains a source snapshot and is not loaded by Central.
- The draft had no normalized exact duplicates within itself or against the pre-import active catalog. Its bubble-fit audit is intentionally separate: 1,108/1,600 localized rows fit, 384/800 pairs fit in both locales, and 416 imported pairs need shortening or review before both locales can be enabled.
- README_TH.md and bubble_fit_report.json in that folder describe the review workflow and exact overflow IDs. The import report and pre-import backup are in LOCAL_REVIEW/DIALOGUE_CATALOG_IMPORT_20260831/. No runtime code, release package or source asset changed.
- For easier author review, `outputs/01a0571c-f344-7b90-927f-ddb3389a1d36/dialogue_catalog_review.xlsx` is a presentation-only export with three visible columns (`mode`, `EN`, `TH`), one row per phrase pair, frozen headers and filters. It contains the same 800 pairs and does not replace the CSV source.

## Completed review-only Work assembly reference

- Added `TOOLS/render_work_frame_reference.py`, a presentation-only renderer that reads the canonical action, frame, asset and Work pose registries and writes review artifacts under `outputs/01a057c6-2d38-7be3-bfbe-4cadb1b6cc66/`.
- The reference character is the existing `RND_F_004` / Josephine Brooks composition: `character.body.001` (`body/body_001.png`) + `character.face.011` (`face/face_011.png`). The native Work recipes are `SE: M20–M24 + M42–M43` and `NW: M25–M29 + M44–M45`; `SW: Mp20–Mp24, Mp42–Mp43` mirrors SE and `NE: Mp25–Mp29, Mp44–Mp45` mirrors NW at the complete-frame level.
- The component sheet shows all 14 native Work frames as body crop + head/face crop = final 32×42 frame, including the new fixed-head/alternating-body composites and their source rectangles/canvas destinations. `B1–B6` and `H1–H6` are sheet-only labels; individual crops have no separate canonical asset IDs beyond their frame-rule `src` rectangles.
- The body catalog contains all 23 registered body assets and the head/face catalog contains all 30 registered face assets, with canonical IDs, filenames, dimensions and reference counts. The registries define numeric IDs only; they do not provide human-readable clothing/hair names.
- `work_frame_component_map.csv` is the machine-readable crop/use map for 28 Work frame IDs (14 native + 14 derived), and `body_head_asset_catalog.csv` is the 53-row asset catalog. These are review outputs; the canonical registries remain the runtime source of truth.
- This remains an author-review aid for the visual breakdown, but the author-approved turn recipe is now adopted in the canonical action/frame registry. No character/world artwork, placement, navigation data or reference hash was changed, and no release was promoted.
- The adopted Work turn recipe binds `SE:turn_side_sw` to `M22 → M42` (`B1+H1 → B2+H1`), `SE:turn_side_ne` to `M23 → M43` (`B1+H4 → B2+H4`), `NW:turn_side_sw` to `M27 → M44` (`B4+H1 → B5+H1`) and `NW:turn_side_ne` to `M28 → M45` (`B4+H4 → B5+H4`). The derived SW/NE action frames are final-composite mirrors.

## Author-requested Work direction completeness implementation

- The canonical action registry, character creation facade and review tooling now use only `turn_side_<direction>` names; the temporary two-slot labels were removed from the task-owned sheets/tools. GIF frames expose the exact direction, subaction and numeric `frame_index`.
- The approved canonical turn recipe is fixed head plus alternating body: SE uses `turn_side_sw` = `M22 → M42` (`B1+H1 → B2+H1`) and `turn_side_ne` = `M23 → M43` (`B1+H4 → B2+H4`); NW uses `turn_side_sw` = `M27 → M44` (`B4+H1 → B5+H1`) and `turn_side_ne` = `M28 → M45` (`B4+H4 → B5+H4`). Shared `normal_work` remains unchanged.
- NE character Work is available immediately from NW with `mirror_y`: `normal_work`, `turn_side_se`, `turn_side_nw` and `happy`, using `Mp25–Mp29` plus `Mp44–Mp45`. This adds no duplicate character artwork.
- World/WorkSeat NE is now a reusable derived path rather than a separate unsupported bridge: `WorkSeatCore` derives the complete NW workstation presentation to NE with one `FLIP_LEFT_RIGHT` relation around the source chair canvas, mirrors desk/PC/chair/optional foreground positions and sprites, preserves authored layers, and derives the character/effect/popup offsets consistently. A synthetic future NE assignment against `floor02.ws8` is covered by an exact scene-composition regression; current authored slots remain three-way and no static chair/world asset change is authorized or required.
- The review artifact is a set of animated GIFs, one per character direction/subaction (including the derived NE action set), with visible `direction`, exact `subaction` and numeric `frame_index`; the GIFs remain evidence/cache only and are not canonical source data.

## Verification

- TDD: initial 11 focused catalog cases failed for missing behavior; implementation made them pass. Two additional ambiguous-CSV cases were observed failing before adding the parser guards.
- Fresh dialogue-readiness verification on 2026-08-31: `python -B -m pytest -q TESTS/test_dialogue_bubble.py TESTS/test_dialogue_catalog.py -p no:cacheprovider` — 24 passed in 2.21s. Coverage includes actor movement/frame-bob placement, pixel-fit selection, Central/employee rendering, metadata filtering/JSON, legacy CSV compatibility, malformed content rejection, edit/add/reload, failed reload preserving the old catalog, disabled-row rejection and rendering every enabled office localization.
- Presentation artifact verification on 2026-08-31: rendered and visually inspected `outputs/01a0571c-f344-7b90-927f-ddb3389a1d36/RND_F_004_character_sheet.png` at 1,232×2,034 px; all 47 requested frame labels and the action mapping are present. No source/static asset was changed.
- Presentation artifact verification on 2026-08-31: rendered and visually inspected `outputs/01a0571c-f344-7b90-927f-ddb3389a1d36/RND_F_004_action_direction_sheet.png` at 1,600×1,775 px; rows are action/subaction groups, columns are NE/SE/SW/NW, and directionless event poses are separated below. No source/static asset was changed.
- Direction-named action sheet verification on 2026-08-31: rendered and visually inspected `outputs/01a0571c-f344-7b90-927f-ddb3389a1d36/RND_F_004_action_direction_sheet_v2.png` at 1,600×1,772 px; WorkSeat rows are split by target idle direction (`turn_side_sw`, `turn_side_ne`, `turn_side_se`, `turn_side_nw`) and blank cells show unsupported direction/subaction combinations. No source/static asset was changed.
- Work-turn naming implementation verification on 2026-08-31: `gds_standard_v1.json` and `work_pose_profiles.json` now use `turn_side_<direction>` names (`sw`, `ne`, `se`, `nw`) with `U+/U-/V+/V-` axis conventions; `WorkSeatCore.resolve_turn_side_mapping()` validates the direction name, axis and UV delta; `resolve_turn_side_for_target()` selects the named turn from a known partner-relative idle direction; Central exposes both resolvers; every derived interaction slot carries the mapping. Focused WorkSeat profile/lifecycle/runtime suite passes 36/36, with the full regression at 212/212. No walking, participant lock or automatic facing-coordination loop was implemented.
- Latest full root regression on 2026-08-31: `python -B -m pytest -q -p no:cacheprovider` — 212 passed. No navigation/world change occurred.
- Central integrity: PASS; all 26 schemas pass, 502 referenced payloads have zero mismatches, all 25 floor PNG/RGBA checks pass, 302 character identities and 219 workstations resolve. `release_clean=false` because the development tree contains 199 Python cache paths; no package was promoted.
- Import verification: the approved expansion import appended all 800 EN/TH pairs and preserved existing rows exactly; 2,009 active CSV rows, 1,005 IDs, 1,347 enabled rows and 492 fit-gated disabled rows. Actual validated reload returns 2,009 rows / 1,005 IDs / locales en, th. The import report is `LOCAL_REVIEW/DIALOGUE_CATALOG_IMPORT_20260831/import_report.json`.
- Draft expansion verification: build_draft.py generated 800 unique phrase pairs / 1,600 rows across 16 office categories; normalized duplicate and active-catalog collision checks passed. validate_draft.py checked every localized row against the real renderer and wrote the review-required fit report.
- Earlier independent integration review, before the expansion: no actionable findings; the then-current 239 enabled rows rendered safely. The fresh focused run above covers the current enabled office catalog. Technical checks do not constitute visual/Thai author acceptance or release approval.
- Final source/static audit: all 3,388 extracted source files, 168 character asset files and 329 world asset files match the pre-task byte counts and tree hashes. Scoped git diff --check passes; no task-owned long-running process remains because none was started.
- Work reference verification on 2026-08-31: `python -B TOOLS/render_work_frame_reference.py --character RND_F_004 --output-dir outputs/01a057c6-2d38-7be3-bfbe-4cadb1b6cc66` completed with registry hash verification; output map has 28 Work frame rows (`M20–M29`, `M42–M45`, `Mp20–Mp29`, `Mp42–Mp45`), the asset catalog has 23 body + 30 face rows, and the generated manifest records PNG dimensions/hashes. `body_001.png` and `face_011.png` both match their canonical registry SHA-256 values.
- Canonical Work sheet verification on 2026-08-31: `python -B TOOLS/render_proposed_work_action_sheet.py --character RND_F_004 --output-dir outputs/01a057c6-2d38-7be3-bfbe-4cadb1b6cc66` completed with registry hash verification. The sheet includes all standard action groups, four direction columns and exact `turn_side_<direction>` rows; the comparison includes eight legacy/canonical side-turn rows and the map uses direction/frame-index labels with no temporary two-slot labels. Both sheets were visually inspected.
- Work character GIF verification on 2026-08-31: `python -B TOOLS/render_work_character_gifs.py --character RND_F_004 --output-dir outputs/01a057c6-2d38-7be3-bfbe-4cadb1b6cc66` produced one two-frame master sheet GIF plus 16 per-direction/subaction GIFs for `NE/SE/SW/NW`, including NE derived from NW and SW derived from SE. Frame 0/1 PNG snapshots were visually inspected; the GIF tool remains read-only while the canonical action/frame update is now committed in source.
- NW→NE workstation mirror preview on 2026-08-31: the review-only `TOOLS/render_ne_workstation_mirror_gif.py` reproducibly sampled `floor02.ws8` (`desk_002.part_01`, `pc_002.slot_01`, `chair_002.part_00` plus `chair_002.part_03` foreground) and `RND_F_064`, composed the complete NW workstation, then applied one final-composite `mirror_y` to produce NE across `normal_work`, `turn_side_se`, `turn_side_nw` and `happy`. The 7-frame GIF and first-frame/turn-frame PNGs were visually inspected; this evidence was followed by the canonical runtime integration and remains a review artifact only.
- Canonical Work action verification on 2026-08-31: focused tests cover the four-way registry, fixed-head/alternating-body frame rules, all 16 Work direction/subaction renders and exact SW/NE final-frame mirrors. Full root regression after NE WorkSeat integration: `python -B -m pytest -q -p no:cacheprovider` — 221 passed in 49.59s.
- Derived NE WorkSeat verification on 2026-08-31: `WorkSeatCore` resolves NE from NW, mirrors the complete local chair composite and future `floor02.ws8` world components (desk `desk_002.part_01`, PC `pc_002.slot_01`, chair `chair_002.part_00`, foreground `chair_002.part_03`) using source layers/positions and one mirror relation. Effects, HumanBall offsets and VFX presentation also derive NE from NW without double-mirroring. The exact synthetic future-NE scene test passes.
- Central integrity on 2026-09-01: PASS; all 28 schemas pass, 502 referenced payloads have zero mismatches, action resolution is 9,060 requests / 17,516 frame occurrences, all 25 floor PNG/RGBA checks pass, 302 character identities and 219 workstations resolve. `release_clean=false` because the development tree contains 205 Python cache paths; no package was promoted.
- WorkSeat audit on 2026-08-31: PASS; all 219 current workstation compositions remain renderable, static floor/chair hashes remain exact, all 2,114 SW and 2,114 NE character mirror pairs pass, the four NE readiness compositions pass, and the synthetic NE world-component probe passes. The audit's Room Navigation regression also passes. Current slot direction counts remain `SE=100`, `NW=96`, `SW=23`, `NE=0`.
- Required navigation/spatial verification on 2026-08-31: Room Navigation PASS (25 floors), Navigation Occupancy PASS (25 floors / 219 workstations), Phase 6 Spatial PASS (224 profiles / 681 primary objects), F2 gameplay-metadata family PASS (23-floor family, zero mismatches), and WorkSeat Lifecycle PASS (219 slots / 9 canonical cycles).
- Read-only conversation-space probe on 2026-08-31: all 219 WorkSeat slots and their runtime-derived navigation data were inspected without materializing occupancy caches. `218/219` slots have a reachable walkable cell on both exact side-axis lanes supported by their Work turn; the nearest cell is consistently five fine-grid cells beyond the chair footprint. `floor00.ceo` is the sole exact-lane exception, but its red outward/front depth envelope still has 30 reachable target cells (25 on floor01 and 25 on floor02), so the CEO host path does not need a side-turn guess. The 436 exact-lane candidates have no duplicate cells, no pair within the current radius-one crowd reservation conflict distance and none within two cells of a portal; 50 candidates coincide with that same workstation's ingress gate and therefore need a shared resource lock. Only `144/219` existing transition gates have a dominant direction supported by the seat's side turn; `75/219` do not, confirming that ingress gates must not be reused as universal talk positions. A standing-pair probe using V-axis endpoints (`u` equal, `v` gap 4) and a two-cell open navigation ring found `877`/`1,355`/`1,394` candidate pairs on the floor00/floor01/floor02 geometry families respectively (with hundreds still available at gaps 5–6). This is a geometry-readiness analysis, not visual author acceptance or a canonical talk-slot implementation.
- Wall-buffer desk audit on 2026-08-31: `26/219` WorkSeat desks have effective desk clearance touching a room boundary; chair clearance itself is boundary-relieved, so this is a desk-buffer condition rather than 26 chairs being flush to a wall. Of those 26, `25/26` still have both exact side-axis standing candidates walkable and off the room boundary (50 candidates total); `floor00.ceo` has neither exact side candidate and therefore uses the CEO `ceo_front`/red-front policy rather than a side-turn guess. The passing candidates sit one fine-grid cell outside the clearance union with usable nearby navigation; no static geometry, placement or cache was changed. This confirms that a desk buffer touching a wall does not by itself block a seated-host conversation, but sprite-depth/bubble overlap still needs visual QA.
- Conversation planning update on 2026-08-31: the active ROADMAP now freezes the author-directed first-slice state/policy, UV axis and inverse-facing rules, CEO front and employee outward-side resolvers, atomic lock order, cancellation/return sequence, Central-facing operations, implementation order and acceptance matrix. Duration, loop count, speaker cadence, seeded category weights and talk stamina tuning remain deliberately deferred. This was documentation-only; no runtime tests were required beyond `git diff --check`.
- Conversation vertical-slice implementation on 2026-08-31: added and schema-validated `CONTRACTS/conversation_behavior.json`; added Central operations for snapshot/spot/plan/advance/cancel; implemented deterministic V-first standing pairs, CEO red/front host-only plans, explicit self-talk and outward-side seated-host resolution; added five focused contract/geometry/state tests. Full regression `python -B -m pytest -q -p no:cacheprovider` — 226 passed. New `VALIDATION/self_audit_conversation.py` — PASS across all 25 floors (standing pair and CEO front plan readiness, walkability, inverse facing and seated CEO channel). Existing Room Navigation, Navigation Occupancy, Phase 6 Spatial, WorkSeat Lifecycle, WorkSeat and Central integrity audits all PASS; F2 gameplay-metadata family audit PASS. GIF review evidence was rendered under `LOCAL_REVIEW/PHASE8E_CONVERSATION_QA_20260831/` for floor02 employee pair, floor02 employee→CEO and floor01 employee pair; timing/loop labels are preview-only.
- Conversation timing seam on 2026-08-31: Central now accepts explicit talk duration/frame input, bounded loop count and deterministic alternating speaker turns; tracks carry speaker/listener/loop/turn metadata and the renderer shows one active bubble per turn. Seven focused conversation tests pass, including two-loop determinism and timing-conflict rejection. Refreshed GIF manifest remains PASS and uses two preview loops for speaker review; final gameplay duration, phrase selection and stamina deltas remain pending tuning.
- Latest source audit on 2026-08-31: no temporary two-slot turn labels remain in canonical character/contract/runtime/world data or the task-owned Work review tools; the character-direction bridge is four-way and NE derives from NW. No navigation/world geometry or static assets changed, and no development server or canonical release was started.
- Task-specific review/verification evidence is under LOCAL_REVIEW/DIALOGUE_CATALOG_INTEGRATION_20260831/. The before-code snapshot distinguishes this work from existing uncommitted Phase 8E changes.
- Current conversation timing/content verification on 2026-09-01: full root regression `python -B -m pytest -q -p no:cacheprovider` — 233 passed; the focused conversation suite — 12 passed; `self_audit_conversation.py` PASS across all 25 floors; Central integrity PASS (28 schemas); gameplay-metadata family PASS (23 floors). Refreshed review GIFs and contact sheets under `LOCAL_REVIEW/PHASE8E_CONVERSATION_QA_20260901/` use the 60ms simulation tick and show first speaker → +500ms partner → shared fade → return. No navigation/world geometry or static assets changed; no development server or canonical release was started.
- Bubble anchor audit on 2026-09-01: the walking actor anchor itself was correct (bubble tail followed the visible head with the existing one-pixel vertical baseline), but `standing_pair` had applied a presentation-only `±30px/-4px` fan-out after rendering, displacing each tail by `±30px` horizontally. The author then selected the simpler direct-anchor rule. Pair offsets are now explicitly `[0, 0]`; the GIF renderer composites each whole-crop bubble at the exact head-derived top-left with no body movement, collision search or connector. For seated/CEO/self-talk bubbles it composes the WorkSeat human at the direction-specific visual offsets `SE=(2,2)`, `NW=(-10,-6)` and `SW=(-13,2)` before resolving the head anchor. This is a renderer/runtime correction only; no asset or geometry changed. The author accepted the refreshed GIFs on 2026-09-01.
- Draw-order/y-sort audit on 2026-09-01: the compositor still has three distinct policies, not one global y-sort queue. `LayoutCore/FloorRenderer` composites static floor placements by authored `layer`; `WorkSeatCore` composites chair/desk/PC/human events by `(layer, order, workstation_id, kind)`; only walking actors use ground-Y sorting in `WalkingDepthCore` (and the QA renderer mirrors that sort) before world-occluder masking. Conversation bubbles remain a post-actor overlay painted in speaker order, independent of world y-sort. If the two bubble rectangles overlap, the later speaker is intentionally composited last and covers the earlier pixels; there is no displacement, collision resolver or depth-based bubble placement.
- Read-only PC atlas audit on 2026-09-01 (pre-integration snapshot): all 36 source `office/pc_*.png` sheets are `100x96` RGBA and contain six `50x32` cells. Pixel matching confirms source `cell0` is the active `slot_00` / `SE` image and source `cell1` is the active `slot_01` / `NW` image for all 25 wired PC families; source `cell2`–`cell5` are four additional distinct NW screen states (five NW cells total). Current authored workstation counts remain `SE=100`, `NW=96`, `SW=23`, `NE=0`.
- Isolated PC animation POC on 2026-09-01: `TOOLS/render_floor06_pc_animation_poc.py` uses the authored floor06 roster (9 employees / 9 workstations), production movement paths and synchronized-head crowd scheduling, then renders review-only GIFs under `LOCAL_REVIEW/PHASE8E_PC_ANIMATION_POC_20260901/`. NW workstations advance `cell1→cell2→cell3→cell4→cell5` after each two-frame `normal_work` loop (720ms under the canonical timing policy); SE/SW remain `cell0`. Atlas checks pass (`cell0/slot_00` and `cell1/slot_01` byte-equal; five NW cells distinct), crowd schedule is collision-free, and the POC was author-approved for integration.
- Canonical PC animation integration on 2026-09-01: `WORLD/REGISTRY/pc_animation.json` now maps all 25 active PC families to one static `slot_00` plus five recovered animated `slot_01..05` assets/normal variants (100 new content-addressed blobs). `WorkSeatCore` resolves NW/NE frames and mirrors NE through the existing composite relation; SE/SW retain their authored static variant. `WorkSeatLifecycle` advances the PC index from elapsed seated time after each complete work-action loop, independently of character/effect/HumanBall channels, and `CentralGameCore.resolve_workstation_pc_frame()` exposes the mapping. Focused PC tests (including future-NE), full regression (`239 passed`), Central integrity (29 schemas), WorkSeat, Room Navigation, Navigation Occupancy, Phase 6 Spatial and F2 gameplay-metadata audits all pass. No floor geometry changed, all 25 static floor PNG/RGBA hashes remain exact, and no release was promoted.
- Timing comparison review on 2026-09-01: generated three read-only pre-migration floor06 GIF variants under `LOCAL_REVIEW/PHASE8E_PC_ANIMATION_COMPARE_20260901/` — baseline `220/140`, tick-aligned `240/180`, and tick-aligned `300/180` (character/VFX-HumanBall policy labels). The reused floor06 PC POC had no effect or HumanBall assignment, so the visible comparison exercised character and NW PC-cell cadence; SE/SW remained static cell0. The variants used the same compact 2× preview scale, canonical atlas verification and production movement schedule.
- Effect timing review on 2026-09-01: generated a read-only floor06 composition under `LOCAL_REVIEW/PHASE8E_PC_ANIMATION_EFFECT_POC_20260901/` with `character=360ms`, `effect=240ms`, `HumanBall policy=240ms`, `fire_original` assigned to every seated floor06 character, and the canonical effect-layer compositor enabled. The GIF shows effects only after each actor reaches its workstation; no HumanBall popup was assigned in that pass. This was the pre-migration review override; the canonical registries/contracts are now updated to the same 360/240/240 policy.
- Canonical timing-policy migration and author approval on 2026-09-01: `CONTRACTS/work_seat_lifecycle.json`, its schema, WorkSeat lifecycle defaults, all 11 effect animation metadata entries, HumanBall metadata/schema, character render/export defaults and timing QA tools now use character `360ms`, VFX `240ms`, HumanBall `240ms` on the shared `60ms` tick. PC cadence remains derived (`720ms` for two-frame Work actions); the 24-tick Work duration remains `1,440ms`. Character manifest digests and timing metadata were regenerated, floor06 Work/VFX/HumanBall reports were refreshed, the migration regression was `239 passed`, and the latest full root regression after the actor-contract slice is `247 passed`; all required navigation/WorkSeat/lifecycle/Phase 6/Central/F2 audits pass, and no static artwork/world geometry changed. Review evidence is under `LOCAL_REVIEW/PHASE8E_PC_ANIMATION_POC_20260901/`, `LOCAL_REVIEW/PHASE8E_TIMING_POLICY_PREVIEW_20260901/` and `LOCAL_REVIEW/PHASE8E_TIMING_POLICY_QA_20260901/`; the author-acceptance gate is closed and no release was promoted.
- Actor simulation contract and reducer slice on 2026-09-01: added `CONTRACTS/actor_simulation.json`, `SCHEMA/actor_simulation.schema.json`, the runtime snapshot schema `SCHEMA/actor_snapshot.schema.json` and `RUNTIME/actor_simulation_core.py`, linked the contract from `CONTRACTS/central_contract.json`, exposed the JSON-safe reducer through Central, and added a central schema-pair audit plus 8 contract/snapshot and 9 reducer tests. The contract freezes assigned-actor snapshot fields, `home/entering/present/leaving` presence, `walking_to_work/working/talking/wandering/popup_event/going_home/home_recovery/returning_to_work` activity, integer milli-stamina (`100000` max; low `30000`; critical `10000`), existing recovery weights (`45/20/15/20`), deterministic counters/seeds, and explicit assignment/renderer/storage boundaries. The reducer now performs exact working drain with remainder carry, emits each downward threshold crossing once, deterministically chooses weighted recovery events, applies deterministic clamped recovery, and accepts explicit `request_home` without changing workstation ownership. Full regression is `256 passed`; Central integrity is PASS with 30 contract/data schemas and `release_clean=false` only because development Python-cache paths remain. Home/return routing and binding the reducer to participant conversation/movement remain pending; no floor geometry or static asset change was included.
  - Speech scheduler and cross-channel conversation slice on 2026-09-01: added `CONTRACTS/speech_scheduler.json`, `SCHEMA/speech_scheduler.schema.json`, `SCHEMA/speech_scheduler_snapshot.schema.json` and `RUNTIME/speech_scheduler_core.py`, linked them from Central and exported the scheduler facade. Focused scheduler coverage is `9 passed`; the full root regression is `265 passed`. The scheduler keeps solo categories (`encouragement`, `uncertainty`, `surprise`, `work_progress`, `idle_flavor`), pair categories (`conversation_open`, `conversation_reply`) and lifecycle one-shots (`greeting`, `work_start`, `fatigue`, `leaving`) separate; quantizes random intervals to the shared 60ms tick; blocks a floor lane through the 4,000ms visible window plus 300ms fade; emits a distinct bubble-start event at the movement arrival boundary; and never mutates action/pose, stamina or workstation assignment. Automatic pair selection filters same-floor available actors, seeds the three modes (`ceo_front`, `seated_host`, `standing_pair`) and retries only plans with valid partner/spot/dialogue data. Standing pairs receive one shared deterministic d6 outcome (even `happy`, odd `sad`), hold both poses for 1,200ms after fade and then emit the return hook. `CentralGameCore.resolve_runtime_snapshot()` / `advance_runtime_snapshot()` compose independent actor, speech and conversation channels; `actor_draws_over_reception()` uses the authored reception depth envelope for the one-shot leaving trigger. Room Navigation, Navigation Occupancy, WorkSeat, WorkSeat Lifecycle, Phase 6 Spatial, Central integrity, F2 gameplay-metadata family and conversation audits all PASS; `release_clean=false` only reflects development Python-cache paths. No floor geometry or static asset change was included; visual/behavior author acceptance of this scheduler integration and full home/return binding remain open.
  - Final reducer/presentation verification on 2026-09-01: the focused actor/home-return, contract, scheduler, renderer and host-loop tests pass; direct checks cover 60ms route samples, effective reception-depth leaving timing, talk-to-pair bridging, independent VFX/HumanBall bindings, portal despawn/restore, ready-gated return and same-assignment WorkSeat re-entry. The latest full root regression `python -m pytest -q` is **280 passed in 83.58s**. Room Navigation, Navigation Occupancy, WorkSeat, WorkSeat Lifecycle, Phase 6 Spatial, Central integrity, F2 gameplay-metadata family and conversation audits all PASS after the renderer/host-loop contract updates. `git diff --check` passes with only Git's normal LF→CRLF warnings; no development server or release archive was started, and `release_clean` remains false only for development cache paths.
  - Actor/presentation/home-return binding on 2026-09-01: actor routes now reuse the canonical WorkSeat transition gate, Pathfinding/CharacterMovement timeline and four-step portal fade. `request_home` walks to the portal, exits into `home_recovery`, restores full stamina at the despawn boundary, waits for the per-employee ready time and accepts only an explicit `request_return`; return re-enters through the portal and walks to the same owned gate before restoring `present/working`. Wander uses a short deterministic out-and-back movement route. `behavior_started.presentation` binds talk to the speech scheduler and background effects/popup/movement to `conversation`, VFX, HumanBall and walking-depth channels without changing stamina or assignment. Central forwards actor talk, effective reception-depth and workseat-reentry timestamps and exposes the read-only runtime presentation snapshot; `RuntimePresentationRenderer` consumes it with independent per-assignment frame channels, walking-depth masking, direct head-anchor bubble paint and lifecycle-route preservation. `RuntimePresentationLoop` now owns the host-side current snapshot copy, advances Central and renders one floor in a single tick result without mutating the caller's source snapshot; a failed tick keeps the last good snapshot. Focused actor/contract/scheduler/presentation coverage is 41 tests. No static world/character assets or geometry changed.
  - Runtime presentation QA on 2026-09-01: `python TOOLS/render_runtime_presentation_qa.py` completed with `status=PASS`, and now drives `RuntimePresentationHostAdapter.tick()` → `RuntimePresentationLoop.tick()` for every sample. It produces contact sheets for `ceo_front`, `seated_host`, `standing_pair` and home/return under `LOCAL_REVIEW/PHASE8E_RUNTIME_PRESENTATION_QA_20260901/`. Visual checks confirm the +500ms partner bubble, shared 4,000ms window/300ms fade, standing-pair emotion hold/return, seated turn-side/CEO normal-work poses and fatigue bubble while walking through the authored reception depth. The renderer's per-assignment character/VFX/HumanBall/PC indices are covered by regression tests; no static world/character asset or geometry changed.
- External host boundary implementation on 2026-09-01: added `RUNTIME/runtime_presentation_host.py` and exported `RuntimePresentationHostAdapter`. It calls `RuntimePresentationLoop.tick()` exactly once per host frame, forwards one defensive frame to `frame_sink`, dispatches ordered runtime events to `event_sink`, supports redraw through `render_current()` without advancing clocks, and rejects re-entrant calls. Contract/schema entries were added and focused presentation/host tests now pass 12/12; the full root regression is `283 passed`. The repository has no windowing/UI participant loop, so this is the completed in-repo seam; wiring it into the external app and visual author acceptance remain open.

## Next task and open gates

1. Completed and author-approved on 2026-09-01: review the refreshed conversation GIFs for exact head anchoring, intentional later-speaker overwrite, seated/CEO anchoring and the existing one-loop timing.
2. Embed `RuntimePresentationHostAdapter.tick()` into the external app's live participant loop while preserving the completed conversation geometry, one-loop timing and assignment contract. The in-repo adapter, read-only track/bubble/VFX/HumanBall/frame-clock bridge and review QA consumer are implemented; the repository has no app/window host, so external wiring, numeric sad/happy stamina tuning and explicit visual/behavior author acceptance remain open.
3. Home/return composition is implemented and assignment-safe; next validation must cover live tick persistence and visual portal/WorkSeat review. `request_return` remains explicit and ready-gated; text rendering must remain side-effect free.
4. Keep `CHARACTER/DIALOGUE/dialogue.csv` as the editable source. Re-enable fit-gated rows only after renderer fit/reload checks.
5. Add authored NE workstation direction records only when a real NE world slot is introduced; CEO outbound, group and walk-and-talk modes remain excluded/follow-up behavior.
6. Review remaining Thai shaping/content details under `LOCAL_REVIEW/PHASE8E_CONVERSATION_QA_20260901/` as a separate content-quality follow-up. Pillow has no RAQM, so pixel fit is not a guarantee of Unity-equivalent Thai shaping.
7. Replace temporary TV bubble/font development inputs with project-owned artwork/font policy before canonical release promotion. No release was promoted in this task.

No technical blocker for catalog use or editing. Do not mark Phase 8E closed from the CSV, tests or generated reports alone.
